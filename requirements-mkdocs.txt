# Documentation build requirements, resolved 2026-09-02 and verified by building
# the site with `mkdocs build --strict` on Python 3.12.
#
# Direct requirements -- everything else below is a transitive dependency of one
# of these, pinned so a build is reproducible rather than resolved fresh:
#
#   mkdocs                                     the site generator, and the search plugin
#   mkdocs-material                            the theme named in mkdocs.yml
#   mkdocs-minify-plugin                       the `minify` plugin
#   mkdocs-git-revision-date-localized-plugin  the `git-revision-date-localized` plugin
#   pymdown-extensions                         the `pymdownx.*` markdown extensions
#
# Pins are exact. pip has no release-age gate, so maturity is a manual check:
# every version below was confirmed published more than seven days before it was
# adopted, and confirmed not yanked. Two were held one release back from the
# newest for that reason rather than adopted inside the window.
#
# Regenerate by installing the five direct requirements into a clean virtual
# environment, running `pip freeze`, and re-checking maturity and yank status
# before committing the result.
babel==2.18.0
backrefs==8.0
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.5.0
colorama==0.4.6
csscompressor==0.9.5
ghp-import==2.1.0
gitdb==4.0.12
GitPython==3.1.60
htmlmin2==0.1.13
idna==3.19
Jinja2==3.1.6
jsmin==3.0.1
Markdown==3.10.3
MarkupSafe==3.0.3
mergedeep==1.3.4
mkdocs==1.6.1
mkdocs-get-deps==0.2.2
mkdocs-git-revision-date-localized-plugin==1.5.4
mkdocs-material==9.7.7
mkdocs-material-extensions==1.3.1
mkdocs-minify-plugin==0.8.0
packaging==26.3
paginate==0.5.7
pathspec==1.1.1
platformdirs==4.11.4
Pygments==2.21.0
pymdown-extensions==11.0.2
python-dateutil==2.9.0.post0
PyYAML==6.0.3
pyyaml_env_tag==1.1
requests==2.34.2
six==1.17.0
smmap==5.0.3
urllib3==2.7.0
watchdog==6.0.0
